mysql> use employee
mysql> show tables;
mysql> show databases;
mysql> use employees;
mysql> show databases;
mysql> use employees;
mysql> show tables;
mysql> create table course (
mysql> course_id integer,
mysql> course_name varchar(50),
mysql> course_description varchar(100));
mysql> create table course ( course_id integer, course_name varchar(50), course_description varchar(100));
mysql> show tables;
mysql> describe course;
mysql> alter table course add course_day varchar(50);
mysql> desc course;
mysql> desc salaries;
mysql> select distinct salary from salaries where salary between 40000 and 60000;
mysql> select salary from salaries where salary between 40000 and 60000;
mysql> show tables;
mysql> desc employees
mysql> desc employees;
mysql> desc employees desc employees;
mysql> desc employees;
mysql> select hire_date from employees where hire_date > '01-01-1995';
mysql> select * from employees where hire_date > '01-01-1995';
mysql> select * from employees where hire_date > '1995-01-01';
mysql> show table;
mysql> show table show table;
mysql> show table;
mysql> show tables;
mysql> desc salaries;
mysql> select salary from salaries where salary between 60000 and 90000;
mysql> select * from salaries where salary between 60000 and 90000;
mysql> show tables;
mysql> desc titles;
mysql> select * from titles where title = 'engineer';
mysql> select distinct title from titles where title like '%engineer%';
mysql> select count(*) from titles where title = 'engineer';
mysql> desc employees;
mysql> desc titles;
mysql> select first_name, last_name, title
mysql> from employees join titles
mysql> on employees.emp_no = titles.emp_no
mysql> where title = 'engineer'
mysql> ;
mysql> select first_name, last_name, title from employees join titles  on employees.emp_no = titles.emp_no where title = 'engineer';
mysql> select first_name, last_name, title, salary from employees join titles  on employees.emp_no = titles.emp_no join salaries on salaries.emp_no = employees.empno where title = 'engineer';
mysql> select first_name, last_name, title, salary from employees join titles  on employees.emp_no = titles.emp_no join salaries on salaries.emp_no = employees.emp_no where title = 'engineer';
mysql> select distinct first_name, last_name, title, salary from employees join titles  on employees.emp_no = titles.emp_no join salaries on salaries.emp_no = employees.emp_no where title = 'engineer';
